name: LogiIdentity CI

on:
  pull_request:
    paths:
      - ".github/workflows/identity-ci.yml"
      - "apps/identity-api/**"
      - "apps/identity-worker/**"
      - "databases/identity/**"
      - "packages/auth/**"
      - "packages/logger/**"
      - "package.json"
      - "package-lock.json"
      - "tsconfig.json"
  push:
    branches: [master, main]
    paths:
      - ".github/workflows/identity-ci.yml"
      - "apps/identity-api/**"
      - "apps/identity-worker/**"
      - "databases/identity/**"
      - "packages/auth/**"
      - "packages/logger/**"
      - "package.json"
      - "package-lock.json"
      - "tsconfig.json"

concurrency:
  group: identity-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: identity checks
    runs-on: ubuntu-latest
    env:
      IDENTITY_DATABASE_URL: postgresql://identity:identity@localhost:5438/identity?schema=public
      IDENTITY_ISSUER: logiidentity
      IDENTITY_AUDIENCE: logiflow,logidesk
      REDIS_URL: redis://localhost:6379
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run identity:prisma:generate
      - run: npm run typecheck -w identity-api
      - run: npm run typecheck -w identity-worker
      - run: npm run test -w identity-api
      - run: npm run build -w identity-api
      - run: npm run build -w identity-worker
      - run: npm audit
